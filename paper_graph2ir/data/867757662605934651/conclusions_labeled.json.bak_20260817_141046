[
  {
    "id": "paper:867757662605934651::conclusion_1",
    "global_id": "gcn_f19f310472184243",
    "order": 1,
    "title": "Trojan ticket concept and LTH-based pruning regime for Trojan models",
    "content": "A Trojan ticket is defined as a sparse subnetwork $(m\\odot\\theta)$ obtained by applying iterative magnitude pruning (IMP) to a trained model $\\theta$ that was learned on a potentially poisoned dataset $\\mathcal{D}_\\mathrm{p}$, where $m\\in\\{0,1\\}^d$ is the binary pruning mask and $\\odot$ denotes elementwise product. The proposed LTH-oriented regime for Trojan analysis uses IMP cycles (train dense model on $\\mathcal{D}_\\mathrm{p}$; prune $p\\%$ by magnitude per round; update mask $m$) and examines both (a) the un-rewound pruned subnetwork $(m\\odot\\theta)$ and (b) its $k$-step finetuned variant $(m\\odot\\theta^{(k)})$ (where $\\theta^{(k)}$ denotes $k$ gradient steps of finetuning under $\\mathcal{D}_\\mathrm{p}$, e.g., $k=10$). This regime intentionally avoids using any clean dataset for pruning or finetuning so that the sparsity-induced behavior reflects the interaction between pruning and Trojan-encoded parameters rather than effects of clean-data finetuning.",
    "labeled_segments": {
      "segments": [
        {
          "text": "A Trojan ticket is defined as a sparse subnetwork $(m\\odot\\theta)$ obtained by applying iterative magnitude pruning (IMP) to a trained model $\\theta$ that was learned on a potentially poisoned dataset $\\mathcal{D}_\\mathrm{p}$, where $m\\in\\{0,1\\}^d$ is the binary pruning mask and $\\odot$ denotes elementwise product. The proposed LTH-oriented regime for Trojan analysis uses IMP cycles (train dense model on $\\mathcal{D}_\\mathrm{p}$; prune $p\\%$ by magnitude per round; update mask $m$) and examines both (a) the un-rewound pruned subnetwork $(m\\odot\\theta)$ and (b) its $k$-step finetuned variant $(m\\odot\\theta^{(k)})$ (where $\\theta^{(k)}$ denotes $k$ gradient steps of finetuning under $\\mathcal{D}_\\mathrm{p}$, e.g., $k=10$). This regime intentionally avoids using any clean dataset for pruning or finetuning so that the sparsity-induced behavior reflects the interaction between pruning and Trojan-encoded parameters rather than effects of clean-data finetuning.",
          "label": "assertion",
          "number": 1
        }
      ]
    },
    "labeling_status": "fallback_trivial"
  }
]